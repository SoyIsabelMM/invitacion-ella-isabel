# MongoDB connection (default for local Docker: mongodb://localhost:27017/rsvp)
MONGODB_URI=mongodb://localhost:27017/rsvp

# Allowed origin for CORS (use * only in development)
CORS_ORIGIN=http://localhost:3000

# Server port
PORT=3001
